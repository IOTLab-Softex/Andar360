<% if @password_recovery_html.present? %>
  <%= @password_recovery_html.html_safe %>
<% else %>
  <p>Ol? <%= @resource.email %>!</p>
  <p>Use o link abaixo para definir uma nova senha.</p>
  <p><%= link_to "Redefinir minha senha", @reset_url %></p>
<% end %>

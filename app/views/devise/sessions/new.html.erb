<div class="login-container">
  <div class="logo-mobile">
    <img src="/logo.png" alt="Logo do Projeto" style="width: -webkit-fill-available; filter: drop-shadow(0 4px 14px rgba(0, 0, 0, 0.6));">
  </div>

  <div class="login-box">
    <div class="login-header">
      <img src="/logo.png" alt="Logo do Projeto" class="login-logo">
      <h2 class="login-h2">Login</h2>
    </div>

    <%= form_with model: resource, as: resource_name, url: session_path(resource_name), data: { turbo: false }, local: true do |f| %>
      <div class="login-field">
        <%= f.label :cpf do %>
          <i class="fa-solid fa-address-card"></i> CPF
        <% end %>
        <%= f.text_field :cpf, autofocus: true, autocomplete: "username", class: "login-input js-cpf-mask" %>
      </div>

      <div class="login-field">
        <%= f.label :password do %>
          <i class="fa-solid fa-key"></i> Senha
        <% end %>
        <div class="login-password-wrap">
          <%= f.password_field :password, autocomplete: "current-password", class: "login-input", id: "login-password-input" %>
          <button type="button" id="toggle-login-password" class="login-password-toggle" aria-label="Mostrar senha" aria-pressed="false">
            <i class="fa-regular fa-eye" aria-hidden="true"></i>
          </button>
        </div>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="login-remember">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me do %>
            <i class="fa-solid fa-star"></i> Lembrar-me
          <% end %>
        </div>
      <% end %>

      <div class="login-actions">
        <%= button_tag type: "submit", class: "login-button" do %>
          <i class="fa-solid fa-right-to-bracket"></i> Entrar
        <% end %>
      </div>
    <% end %>

    <div class="login-recovery">
      <button type="button" id="open-recovery-modal" class="login-recovery__link">Esqueci a senha</button>
    </div>
  </div>

  <div class="wallpaper">
    <div></div>
  </div>

  <small style="bottom: 0; position: fixed; right: 6px; color: white;">Versão 1.0.7</small>
</div>

<div id="password-recovery-modal" class="password-recovery-modal" hidden>
  <div class="password-recovery-modal__backdrop"></div>
  <div class="password-recovery-modal__dialog" role="dialog" aria-modal="true" aria-label="Recuperação de senha">
    <button type="button" id="close-recovery-modal" class="password-recovery-modal__close">&times;</button>

    <div id="recovery-step-chooser" class="password-recovery-step is-active">
      <h3>Recuperar acesso</h3>
      <p>Escolha o método mais seguro para recuperar sua senha.</p>

      <div class="password-recovery-choice-grid">
        <button type="button" class="password-recovery-choice" data-recovery-step="support">
          <span class="password-recovery-choice__title">
            <i class="fa-solid fa-headset" aria-hidden="true"></i>
            Enviar solicitacao ao suporte
          </span>
          <span class="password-recovery-choice__text">Solicite ajuda do time responsavel pela redefinicao do acesso.</span>
        </button>

        <button type="button" class="password-recovery-choice" data-recovery-step="facial">
          <span class="password-recovery-choice__title">
            <i class="fa-solid fa-user-check" aria-hidden="true"></i>
            Liberar por reconhecimento facial
          </span>
          <span class="password-recovery-choice__text">Valide seu rosto e depois envie um link seguro para o e-mail cadastrado.</span>
        </button>
      </div>
    </div>

    <div id="recovery-step-support" class="password-recovery-step">
      <h3>Solicitar suporte</h3>
      <p>Confirme seu CPF para liberar o envio da solicitacao ao suporte da redefinicao.</p>
      <div class="password-recovery-actions">
        <button type="button" class="password-recovery-secondary" data-recovery-back>Voltar</button>
      </div>
    </div>

    <div id="recovery-step-facial" class="password-recovery-step">
      <h3>Liberação por reconhecimento facial</h3>
      <div class="password-recovery-steps-bar">
        <div class="password-recovery-step-chip is-active" data-flow-chip="cpf">1. CPF</div>
        <div class="password-recovery-step-chip" data-flow-chip="loading">2. Carregar</div>
        <div class="password-recovery-step-chip" data-flow-chip="camera">3. Reconhecimento</div>
        <div class="password-recovery-step-chip" data-flow-chip="send">4. Enviar link</div>
      </div>

      <div id="recovery-facial-stage-cpf" class="password-recovery-flow-stage is-active">
        <div class="password-recovery-form">
          <label for="recovery-cpf-input">Informe seu CPF</label>
          <div class="password-recovery-form__row">
            <input id="recovery-cpf-input" class="login-input js-cpf-mask" type="text" inputmode="numeric" placeholder="000.000.000-00">
          </div>
        </div>
      </div>

      <div id="recovery-facial-stage-loading" class="password-recovery-flow-stage">
        <div id="recovery-facial-feedback" class="password-recovery-feedback is-pending">
          <span>Informe seu CPF para iniciar a validação.</span>
        </div>
      </div>

      <div id="recovery-facial-stage-camera" class="password-recovery-flow-stage">
        <div id="recovery-camera-feedback" class="password-recovery-feedback is-pending">
          <span>Posicione o rosto para iniciar a validação facial.</span>
        </div>
        <div id="recovery-camera-wrapper" class="password-recovery-camera">
          <iframe id="recovery-camera-frame" src="about:blank" data-camera-src="/camera?recovery=1" allow="camera *"></iframe>
        </div>
      </div>


      <div id="recovery-facial-stage-send" class="password-recovery-flow-stage">
        <div id="recovery-send-feedback" class="password-recovery-feedback is-success">
          <span>Validação facial concluída. Agora envie o link seguro de redefinição.</span>
        </div>
      </div>

      <div class="password-recovery-actions">
        <button type="button" class="password-recovery-secondary" data-recovery-back>Voltar</button>
        <button type="button" id="recovery-primary-action" class="password-recovery-primary">Avançar</button>
      </div>
    </div>
  </div>
</div>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    function applyCpfMask(input) {
      if (!input) return;
      input.addEventListener("input", (event) => {
        let value = event.target.value.replace(/\D/g, "");
        if (value.length > 11) value = value.slice(0, 11);
        if (value.length > 9) value = `${value.slice(0, 3)}.${value.slice(3, 6)}.${value.slice(6, 9)}-${value.slice(9)}`;
        else if (value.length > 6) value = `${value.slice(0, 3)}.${value.slice(3, 6)}.${value.slice(6)}`;
        else if (value.length > 3) value = `${value.slice(0, 3)}.${value.slice(3)}`;
        event.target.value = value;
      });
    }

    document.querySelectorAll(".js-cpf-mask").forEach(applyCpfMask);
    const loginPasswordInput = document.getElementById("login-password-input");
    const toggleLoginPassword = document.getElementById("toggle-login-password");

    toggleLoginPassword?.addEventListener("click", () => {
      if (!loginPasswordInput) return;
      const showing = loginPasswordInput.type === "text";
      loginPasswordInput.type = showing ? "password" : "text";
      toggleLoginPassword.setAttribute("aria-pressed", String(!showing));
      toggleLoginPassword.setAttribute("aria-label", showing ? "Mostrar senha" : "Ocultar senha");
      toggleLoginPassword.innerHTML = showing
        ? '<i class="fa-regular fa-eye" aria-hidden="true"></i>'
        : '<i class="fa-regular fa-eye-slash" aria-hidden="true"></i>';
    });

    const modal = document.getElementById("password-recovery-modal");
    const openBtn = document.getElementById("open-recovery-modal");
    const closeBtn = document.getElementById("close-recovery-modal");
    const chooserStep = document.getElementById("recovery-step-chooser");
    const supportStep = document.getElementById("recovery-step-support");
    if (supportStep) {
      supportStep.innerHTML = `
        <h3>Solicitar suporte</h3>
        <p>Confirme seu CPF para liberar o envio da solicitacao ao suporte.</p>
        <div class="password-recovery-steps-bar">
          <div class="password-recovery-step-chip is-active" data-support-chip="cpf">1. CPF</div>
          <div class="password-recovery-step-chip" data-support-chip="loading">2. Carregar</div>
          <div class="password-recovery-step-chip" data-support-chip="send">3. Enviar solicitacao ao suporte</div>
        </div>
        <div id="recovery-support-stage-cpf" class="password-recovery-flow-stage is-active">
          <div class="password-recovery-form">
            <label for="support-cpf-input">Informe seu CPF</label>
            <div class="password-recovery-form__row">
              <input id="support-cpf-input" class="login-input js-cpf-mask" type="text" inputmode="numeric" placeholder="000.000.000-00">
            </div>
          </div>
        </div>
        <div id="recovery-support-stage-loading" class="password-recovery-flow-stage">
          <div id="recovery-support-feedback" class="password-recovery-feedback is-pending">
            <span>Informe seu CPF para iniciar a solicitacao.</span>
          </div>
        </div>
        <div id="recovery-support-stage-send" class="password-recovery-flow-stage">
          <div id="recovery-support-send-feedback" class="password-recovery-feedback is-success">
            <span>CPF confirmado. Agora voce ja pode enviar a solicitacao ao suporte da redefinicao.</span>
          </div>
        </div>
        <div class="password-recovery-actions">
          <button type="button" class="password-recovery-secondary" data-recovery-back>Voltar</button>
          <button type="button" id="support-primary-action" class="password-recovery-primary">Enviar solicitacao ao suporte</button>
        </div>
      `;
    }
    const facialStep = document.getElementById("recovery-step-facial");
    const primaryActionBtn = document.getElementById("recovery-primary-action");
    const supportPrimaryActionBtn = document.getElementById("support-primary-action");
    const supportCpfInput = document.getElementById("support-cpf-input");
    const supportFeedback = document.getElementById("recovery-support-feedback");
    const supportSendFeedback = document.getElementById("recovery-support-send-feedback");
    const cpfInput = document.getElementById("recovery-cpf-input");
    const loadingFeedback = document.getElementById("recovery-facial-feedback");
    const cameraFeedback = document.getElementById("recovery-camera-feedback");
    const sendFeedback = document.getElementById("recovery-send-feedback");
    const cameraFrame = document.getElementById("recovery-camera-frame");
    const chips = Array.from(document.querySelectorAll("[data-flow-chip]"));
    const supportChips = Array.from(document.querySelectorAll("[data-support-chip]"));
    const stages = {
      cpf: document.getElementById("recovery-facial-stage-cpf"),
      loading: document.getElementById("recovery-facial-stage-loading"),
      camera: document.getElementById("recovery-facial-stage-camera"),
      send: document.getElementById("recovery-facial-stage-send")
    };
    const supportStages = {
      cpf: document.getElementById("recovery-support-stage-cpf"),
      loading: document.getElementById("recovery-support-stage-loading"),
      send: document.getElementById("recovery-support-stage-send")
    };

    let recoveryPayload = null;
    let supportPayload = null;
    let frameReady = false;
    let facialValidated = false;
    let supportValidated = false;
    let verificationInFlight = false;
    let recoveryStageLocked = false;
    let lastRecoveryPhotoBase64 = null;
    let lastRecoveryCaptureMeta = null;
    let recoveryVerifyAttempts = 0;
    let serviceReady = false;
    const MAX_RECOVERY_VERIFY_ATTEMPTS = 3;
    const MAX_SERVICE_STATUS_ATTEMPTS = 20;

    applyCpfMask(supportCpfInput);

    function normalizedCpf(value) {
      return (value || "").replace(/\D/g, "");
    }

    function setStage(step) {
      Object.entries(stages).forEach(([key, element]) => {
        element.classList.toggle("is-active", key === step);
      });
      chips.forEach((chip) => {
        const isCurrent = chip.dataset.flowChip === step;
        const order = ["cpf", "loading", "camera", "send"];
        chip.classList.toggle("is-active", isCurrent);
        chip.classList.toggle("is-done", order.indexOf(chip.dataset.flowChip) < order.indexOf(step));
      });
      modal?.classList.toggle("is-camera-stage", step === "camera");
      syncPrimaryAction();
    }

    function setSupportStage(step) {
      Object.entries(supportStages).forEach(([key, element]) => {
        element.classList.toggle("is-active", key === step);
      });
      supportChips.forEach((chip) => {
        const isCurrent = chip.dataset.supportChip === step;
        const order = ["cpf", "loading", "send"];
        chip.classList.toggle("is-active", isCurrent);
        chip.classList.toggle("is-done", order.indexOf(chip.dataset.supportChip) < order.indexOf(step));
      });
      syncSupportAction();
    }

    function setFeedback(element, message, type = "pending", loading = false) {
      if (!element) return;
      element.innerHTML = `${loading ? '<span class="password-recovery-spinner" aria-hidden="true"></span>' : ""}<span>${message}</span>`;
      element.classList.remove("is-error", "is-success", "is-pending", "is-loading");
      element.classList.add(`is-${type}`);
      element.classList.toggle("is-loading", !!loading);
    }

    function syncPrimaryAction() {
      if (!primaryActionBtn) return;

      if (facialValidated) {
        primaryActionBtn.hidden = false;
        primaryActionBtn.disabled = false;
        primaryActionBtn.textContent = "Enviar link de redefinição";
        return;
      }

      const currentStage =
        Object.entries(stages).find(([, element]) => element?.classList.contains("is-active"))?.[0] || "cpf";

      if (currentStage === "cpf") {
        primaryActionBtn.hidden = false;
        primaryActionBtn.disabled = false;
        primaryActionBtn.textContent = "Avançar";
        return;
      }

      primaryActionBtn.hidden = true;
    }

    function syncSupportAction() {
      if (!supportPrimaryActionBtn) return;

      if (supportValidated) {
        supportPrimaryActionBtn.hidden = false;
        supportPrimaryActionBtn.disabled = false;
        supportPrimaryActionBtn.textContent = "Enviar solicitacao ao suporte";
        return;
      }

      const currentStage =
        Object.entries(supportStages).find(([, element]) => element?.classList.contains("is-active"))?.[0] || "cpf";

      if (currentStage === "cpf") {
        supportPrimaryActionBtn.hidden = false;
        supportPrimaryActionBtn.disabled = false;
        supportPrimaryActionBtn.textContent = "Avancar";
        return;
      }

      supportPrimaryActionBtn.hidden = true;
    }

    let securityResult = null;
    let securityBadge = null;
    let securityConfidence = null;
    let securityScore = null;
    let securityDistance = null;
    let securitySummary = null;

    function ensureSecurityResult() {
      if (securityResult || !stages.send) return;

      securityResult = document.createElement("div");
      securityResult.id = "recovery-security-result";
      securityResult.className = "password-recovery-security";
      securityResult.hidden = true;
      securityResult.innerHTML = `
        <div class="password-recovery-security__icon" aria-hidden="true">✓</div>
        <div class="password-recovery-security__content">
          <strong id="recovery-security-badge" class="password-recovery-security__title">Validacao aprovada</strong>
          <p id="recovery-security-summary" class="password-recovery-security__summary">Reconhecimento facial validado com sucesso. Clique em enviar para receber o link de recuperação.</p>
        </div>
      `;

      stages.send.appendChild(securityResult);
      securityBadge = document.getElementById("recovery-security-badge");
      securitySummary = document.getElementById("recovery-security-summary");
    }

    function resetSecurityResult() {
      ensureSecurityResult();
      if (securityResult) securityResult.hidden = true;
      if (securityBadge) {
        securityBadge.textContent = "Validacao aprovada";
        securityBadge.className = "password-recovery-security__title";
      }
      if (securitySummary) securitySummary.textContent = "Reconhecimento facial validado com sucesso. Clique em enviar para receber o link de recuperação.";
    }

    function renderSecurityResult(payload) {
      ensureSecurityResult();

      if (securityResult) securityResult.hidden = false;
      if (securityBadge) {
        securityBadge.textContent = "Reconhecimento confirmado";
        securityBadge.className = "password-recovery-security__title";
      }
      if (securitySummary) {
        securitySummary.textContent = "Reconhecimento facial validado com sucesso. Clique em enviar para receber o link de recuperação.";
      }
    }

    function openModal() {
      ensureSecurityResult();
      modal.hidden = false;
      document.body.classList.add("v-no-scroll");
    }

    function resetFacialFlow() {
      recoveryPayload = null;
      facialValidated = false;
      frameReady = false;
      verificationInFlight = false;
      recoveryStageLocked = false;
      lastRecoveryPhotoBase64 = null;
      lastRecoveryCaptureMeta = null;
      recoveryVerifyAttempts = 0;
      serviceReady = false;
      cpfInput.value = "";
      setStage("cpf");
      setFeedback(loadingFeedback, "Informe seu CPF para iniciar a validação.", "pending");
      setFeedback(cameraFeedback, "Posicione o rosto e capture uma foto para validação facial.", "pending");
      setFeedback(sendFeedback, "Validação facial concluída. Agora envie o link seguro de redefinição.", "success");
      resetSecurityResult();
      if (cameraFrame) cameraFrame.src = "about:blank";
      frameReady = false;
    }

    function resetSupportFlow() {
      supportPayload = null;
      supportValidated = false;
      if (supportCpfInput) supportCpfInput.value = "";
      setSupportStage("cpf");
      setFeedback(supportFeedback, "Informe seu CPF para iniciar a solicitacao.", "pending");
      setFeedback(supportSendFeedback, "CPF confirmado. Agora voce ja pode enviar a solicitacao ao suporte da redefinicao.", "success");
    }

    function closeModal() {
      modal.hidden = true;
      modal.classList.remove("is-camera-stage");
      document.body.classList.remove("v-no-scroll");
      chooserStep.classList.add("is-active");
      supportStep.classList.remove("is-active");
      facialStep.classList.remove("is-active");
      resetFacialFlow();
      resetSupportFlow();
    }

    resetSupportFlow();

    function goToMainStep(step) {
      chooserStep.classList.toggle("is-active", step === "chooser");
      supportStep.classList.toggle("is-active", step === "support");
      facialStep.classList.toggle("is-active", step === "facial");
      if (step !== "facial") resetFacialFlow();
      if (step !== "support") resetSupportFlow();
    }

    function startRecoveryValidation() {
      if (!frameReady || !recoveryPayload || !serviceReady) return;
      cameraFrame.contentWindow?.postMessage({
        type: "start-recovery-match",
        captureConfig: {
          width: recoveryPayload.photo_width || 300,
          height: recoveryPayload.photo_height || 600
        }
      }, "*");
    }

    function ensureCameraFrameLoaded() {
      if (!cameraFrame) return;
      const baseSrc = cameraFrame.dataset.cameraSrc || "/camera?recovery=1";
      if (cameraFrame.src.includes("/camera?recovery=1")) return;
      frameReady = false;
      cameraFrame.src = `${baseSrc}&t=${Date.now()}`;
    }

    function unlockRecoveryCapture() {
      if (!frameReady || !recoveryPayload) return;
      cameraFrame.contentWindow?.postMessage({
        type: "unlock-recovery-capture"
      }, "*");
    }

    async function waitForRecognitionService() {
      setStage("loading");

      for (let attempt = 1; attempt <= MAX_SERVICE_STATUS_ATTEMPTS; attempt += 1) {
        setFeedback(
          loadingFeedback,
          attempt === 1 ? "Iniciando servico de reconhecimento facial..." : `Aguardando o servico de reconhecimento facial ficar online (${attempt}/${MAX_SERVICE_STATUS_ATTEMPTS})...`,
          "pending",
          true
        );

        try {
          const response = await fetch("<%= facial_status_password_recovery_path %>", {
            method: "GET",
            headers: {
              "Accept": "application/json",
              "X-CSRF-Token": document.querySelector('meta[name=\"csrf-token\"]')?.content || ""
            }
          });

          const payload = await response.json();
          if (response.ok && payload.ok && payload.online) {
            serviceReady = true;
            return true;
          }
        } catch (_error) {
        }

        await new Promise((resolve) => setTimeout(resolve, 1500));
      }

      serviceReady = false;
      setFeedback(loadingFeedback, "O servico de reconhecimento facial nao ficou online a tempo. Tente novamente em instantes.", "error");
      return false;
    }

    openBtn?.addEventListener("click", openModal);
    closeBtn?.addEventListener("click", closeModal);
    modal?.addEventListener("click", (event) => {
      if (event.target.classList.contains("password-recovery-modal__backdrop")) closeModal();
    });

    document.querySelectorAll("[data-recovery-step]").forEach((button) => {
      button.addEventListener("click", () => goToMainStep(button.dataset.recoveryStep));
    });

    document.querySelectorAll("[data-recovery-back]").forEach((button) => {
      button.addEventListener("click", () => goToMainStep("chooser"));
    });

    cameraFrame?.addEventListener("load", () => {
      frameReady = true;
      startRecoveryValidation();
    });

    async function lookupRecoveryCpf() {
      const cpf = normalizedCpf(cpfInput.value);
      if (cpf.length !== 11) {
        setStage("loading");
        setFeedback(loadingFeedback, "Informe um CPF válido para continuar.", "error");
        return;
      }

      if (primaryActionBtn) primaryActionBtn.disabled = true;
      facialValidated = false;
      recoveryStageLocked = false;
      serviceReady = false;
      recoveryPayload = null;
      lastRecoveryPhotoBase64 = null;
      lastRecoveryCaptureMeta = null;
      setStage("loading");
      setFeedback(loadingFeedback, "Carregando perfil para validação...", "pending", true);

      try {
        const response = await fetch("<%= facial_lookup_password_recovery_path %>", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "Accept": "application/json",
            "X-CSRF-Token": document.querySelector('meta[name=\"csrf-token\"]')?.content || ""
          },
          body: JSON.stringify({ cpf: cpf })
        });

        const payload = await response.json();
        if (!response.ok || !payload.ok) {
          setFeedback(loadingFeedback, payload.message || "Não foi possível validar este CPF.", "error");
          return;
        }

        recoveryPayload = payload;
        const serviceOnline = await waitForRecognitionService();
        if (!serviceOnline) return;

        setStage("camera");
        ensureCameraFrameLoaded();
        setFeedback(cameraFeedback, "Perfil carregado. Posicione o rosto e aguarde a captura automatica para validar.", "pending");
        startRecoveryValidation();
      } catch (_error) {
        setFeedback(loadingFeedback, "Falha ao consultar o perfil para recuperação.", "error");
      } finally {
        syncPrimaryAction();
      }
    }

    async function lookupSupportCpf() {
      const cpf = normalizedCpf(supportCpfInput?.value);
      if (cpf.length !== 11) {
        setSupportStage("loading");
        setFeedback(supportFeedback, "Informe um CPF valido para continuar.", "error");
        return;
      }

      if (supportPrimaryActionBtn) supportPrimaryActionBtn.disabled = true;
      supportValidated = false;
      supportPayload = null;
      setSupportStage("loading");
      setFeedback(supportFeedback, "Carregando perfil para solicitacao de suporte...", "pending", true);

      try {
        const response = await fetch("<%= support_lookup_password_recovery_path %>", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "Accept": "application/json",
            "X-CSRF-Token": document.querySelector('meta[name=\"csrf-token\"]')?.content || ""
          },
          body: JSON.stringify({ cpf: cpf })
        });

        const payload = await response.json();
        if (!response.ok || !payload.ok) {
          setFeedback(supportFeedback, payload.message || "Nao foi possivel validar este CPF.", "error");
          return;
        }

        supportPayload = payload;
        supportValidated = true;
        setSupportStage("send");
        setFeedback(supportSendFeedback, payload.message || "CPF confirmado. Agora voce ja pode enviar a solicitacao ao suporte da redefinicao.", "success");
      } catch (_error) {
        setFeedback(supportFeedback, "Falha ao consultar o perfil para o suporte.", "error");
      } finally {
        syncSupportAction();
      }
    }

    async function sendRecoveryResetLink() {
      if (!recoveryPayload || !facialValidated) return;

      if (primaryActionBtn) primaryActionBtn.disabled = true;
      setStage("send");
      setFeedback(sendFeedback, "Enviando link seguro de redefinição...", "pending", true);

      try {
        const response = await fetch("<%= facial_send_reset_password_recovery_path %>", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "Accept": "application/json",
            "X-CSRF-Token": document.querySelector('meta[name=\"csrf-token\"]')?.content || ""
          },
          body: JSON.stringify({ cpf: recoveryPayload.cpf })
        });

        const payload = await response.json();
        setFeedback(sendFeedback, payload.message || "Operação concluída.", response.ok ? "success" : "error");
      } catch (_error) {
        setFeedback(sendFeedback, "Falha ao enviar o link seguro. Tente novamente.", "error");
      } finally {
        syncPrimaryAction();
      }
    }

    async function sendSupportRequest() {
      if (!supportPayload || !supportValidated) return;

      if (supportPrimaryActionBtn) supportPrimaryActionBtn.disabled = true;
      setSupportStage("send");
      setFeedback(supportSendFeedback, "Enviando solicitacao ao suporte...", "pending", true);

      try {
        const response = await fetch("<%= support_request_password_recovery_path %>", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "Accept": "application/json",
            "X-CSRF-Token": document.querySelector('meta[name=\"csrf-token\"]')?.content || ""
          },
          body: JSON.stringify({ cpf: supportPayload.cpf })
        });

        const payload = await response.json();
        setFeedback(supportSendFeedback, payload.message || "Operacao concluida.", response.ok ? "success" : "error");
      } catch (_error) {
        setFeedback(supportSendFeedback, "Falha ao enviar a solicitacao de suporte. Tente novamente.", "error");
      } finally {
        syncSupportAction();
      }
    }

    primaryActionBtn?.addEventListener("click", async () => {
      if (facialValidated) {
        await sendRecoveryResetLink();
      } else {
        await lookupRecoveryCpf();
      }
    });

    supportPrimaryActionBtn?.addEventListener("click", async () => {
      if (supportValidated) {
        await sendSupportRequest();
      } else {
        await lookupSupportCpf();
      }
    });

    function verifyRecoveryPhoto(photoBase64) {
      verificationInFlight = true;
      recoveryStageLocked = true;
      recoveryVerifyAttempts += 1;
      setStage("loading");

      const retrying = recoveryVerifyAttempts > 1;
      setFeedback(
        loadingFeedback,
        retrying ? `Tentando novamente a validacao facial (${recoveryVerifyAttempts}/${MAX_RECOVERY_VERIFY_ATTEMPTS})...` : "Processando a validacao facial no servidor...",
        "pending",
        true
      );

      fetch("<%= facial_verify_password_recovery_path %>", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "Accept": "application/json",
          "X-CSRF-Token": document.querySelector('meta[name=\"csrf-token\"]')?.content || ""
        },
        body: JSON.stringify({ cpf: recoveryPayload.cpf, photo_base64: photoBase64 })
      }).then(async (response) => {
        const payload = await response.json();
        if (!response.ok || !payload.ok) throw { ...payload, status: response.status };

        facialValidated = true;
        setStage("send");
        setFeedback(sendFeedback, payload.message || "Validacao facial concluida. Agora envie o link seguro de redefinicao.", "success");
        renderSecurityResult(payload);
      }).catch((payload) => {
        facialValidated = false;

        if (payload?.retryable && recoveryVerifyAttempts < MAX_RECOVERY_VERIFY_ATTEMPTS && lastRecoveryPhotoBase64) {
          setFeedback(loadingFeedback, "Servico facial indisponivel no momento. Tentando novamente...", "pending", true);
          setTimeout(() => verifyRecoveryPhoto(lastRecoveryPhotoBase64), 1500);
          return;
        }

        if (payload?.retryable) {
          setStage("loading");
          setFeedback(loadingFeedback, payload?.message || "Servico facial temporariamente indisponivel. Tente novamente em instantes.", "error");
          recoveryStageLocked = true;
          return;
        }

        recoveryStageLocked = false;
        setStage("camera");
        setFeedback(cameraFeedback, payload?.message || "Nao foi possivel validar o rosto com seguranca. Posicione o rosto e tente novamente.", "error");
        unlockRecoveryCapture();
      }).finally(() => {
        verificationInFlight = false;
        syncPrimaryAction();
      });
    }

    window.addEventListener("message", (event) => {
      if (event.source !== cameraFrame?.contentWindow || !event.data) return;

      if (event.data.type === "recovery-capture") {
        if (!recoveryPayload || verificationInFlight) return;

        lastRecoveryPhotoBase64 = event.data.photoBase64;
        lastRecoveryCaptureMeta = event.data.meta || null;
        recoveryVerifyAttempts = 0;
        verifyRecoveryPhoto(event.data.photoBase64);
      }

      if (event.data.type === "recovery-match-status" && (verificationInFlight || recoveryStageLocked)) {
        return;
      }

      if (event.data.type === "recovery-match-status") {
        setStage("camera");
        setFeedback(cameraFeedback, event.data.message || "Preparando validação facial...", event.data.matched ? "success" : "pending");
      }

    });
  });
</script>

<style>
  .login-recovery {
    margin-top: 12px;
    text-align: center;
  }

  .login-recovery__link {
    border: 0;
    background: transparent;
    color: #dbeafe;
    font-weight: 600;
    cursor: pointer;
    text-decoration: underline;
  }

  .login-password-wrap {
    position: relative;
  }

  .login-password-wrap .login-input {
    padding-right: 44px;
  }

  .login-password-toggle {
    position: absolute;
    right: 10px;
    top: 50%;
    transform: translateY(-50%);
    width: 30px;
    height: 30px;
    border: 0;
    border-radius: 999px;
    background: transparent;
    color: #000;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: color .15s ease, background-color .15s ease;
  }

  .login-password-toggle:hover {
    color: #000;
    background: rgba(148, 163, 184, 0.18);
  }

  .password-recovery-modal[hidden] {
    display: none !important;
  }

  .password-recovery-modal {
    position: fixed;
    inset: 0;
    z-index: 10060;
    display: grid;
    place-items: center;
    padding: 16px;
    --pr-surface-1: #2f2f2f;
    --pr-surface-2: #353535;
    --pr-surface-3: #3a3a3a;
    --pr-outline: rgba(255, 255, 255, 0.2);
    --pr-outline-strong: rgba(255, 255, 255, 0.34);
    --pr-text-1: #ffffff;
    --pr-text-2: #f3f4f6;
    --pr-text-3: #e5e7eb;
    --pr-primary: #0078d4;
    --pr-primary-hover: #005fa3;
    --pr-focus: rgba(0, 120, 212, 0.45);
  }

  .password-recovery-modal__backdrop {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at 16% 8%, rgba(0, 120, 212, 0.26), transparent 42%),
      radial-gradient(circle at 84% 16%, rgba(0, 95, 163, 0.25), transparent 42%),
      rgba(0, 0, 0, 0.74);
    backdrop-filter: blur(10px);
  }

  .password-recovery-modal__dialog {
    position: relative;
    width: min(92vw, 820px);
    max-height: 90vh;
    margin: 0;
    padding: 18px 18px 16px;
    border-radius: 22px;
    background:
      radial-gradient(circle at 18% -20%, rgba(0, 120, 212, 0.18), transparent 38%),
      radial-gradient(circle at 85% -10%, rgba(0, 95, 163, 0.18), transparent 36%),
      var(--pr-surface-1);
    border: 1px solid var(--pr-outline-strong);
    box-shadow:
      0 30px 84px rgba(2, 6, 23, 0.58),
      inset 0 1px 0 rgba(255, 255, 255, 0.08);
    color: var(--pr-text-1);
    overflow: auto;
  }

  .password-recovery-modal.is-camera-stage .password-recovery-modal__dialog {
    width: min(96vw, 1180px);
    max-height: 94vh;
    padding: 14px;
    border-radius: 20px;
  }

  .password-recovery-modal__dialog::before {
    content: "";
    position: absolute;
    inset: 0 0 auto;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(125, 211, 252, 0.8), transparent);
    opacity: .96;
  }

  .password-recovery-modal__close {
    position: absolute;
    top: 10px;
    right: 10px;
    width: 42px;
    height: 42px;
    border-radius: 999px;
    border: 1px solid var(--pr-outline-strong);
    background: rgba(15, 23, 42, 0.92);
    color: var(--pr-text-1);
    font-size: 26px;
    line-height: 1;
    cursor: pointer;
    transition: background .18s ease, border-color .18s ease, transform .18s ease;
  }

  .password-recovery-modal__close:hover {
    background: #1e293b;
    border-color: rgba(148, 163, 184, 0.52);
    transform: translateY(-1px);
  }

  .password-recovery-modal__close:focus-visible {
    outline: 3px solid var(--pr-focus);
    outline-offset: 2px;
  }

  .password-recovery-step,
  .password-recovery-flow-stage {
    display: none;
  }

  .password-recovery-step.is-active,
  .password-recovery-flow-stage.is-active {
    display: block;
  }

  .password-recovery-step h3 {
    margin: 0 0 6px;
    font-size: 1.08rem;
    color: var(--pr-text-1) !important;
    letter-spacing: .01em;
    text-shadow: 0 2px 12px rgba(2, 6, 23, 0.35);
  }

  .password-recovery-step p {
    margin: 0 0 10px;
    color: var(--pr-text-3) !important;
    font-size: .9rem;
    line-height: 1.5;
  }

  .password-recovery-choice-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    gap: 14px;
  }

  .password-recovery-choice {
    padding: 15px;
    border-radius: 14px;
    border: 1px solid var(--pr-outline-strong);
    background: linear-gradient(180deg, #3f3f3f, #373737);
    color: var(--pr-text-1);
    text-align: left;
    cursor: pointer;
    box-shadow: 0 6px 20px rgba(2, 6, 23, 0.38);
    transition: transform .18s ease, border-color .18s ease, box-shadow .18s ease, background .18s ease;
  }

  .password-recovery-choice:hover {
    transform: translateY(-1px);
    border-color: rgba(0, 120, 212, 0.78);
    background: linear-gradient(180deg, #4a4a4a, #3e3e3e);
    box-shadow: 0 14px 30px rgba(2, 6, 23, 0.42);
  }

  .password-recovery-choice:focus-visible {
    outline: 3px solid var(--pr-focus);
    outline-offset: 2px;
  }

  .password-recovery-choice__title {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-weight: 700;
    margin-bottom: 6px;
    color: var(--pr-text-2) !important;
  }

  .password-recovery-choice__title i {
    color: #5ab0ff;
    font-size: 0.95rem;
    width: 16px;
    text-align: center;
    flex: 0 0 16px;
  }

  .password-recovery-choice__text {
    display: block;
    color: var(--pr-text-3) !important;
    line-height: 1.5;
  }

  .password-recovery-steps-bar {
    display: flex;
    gap: 8px;
    margin: 0 0 10px;
    overflow-x: auto;
    padding-bottom: 4px;
  }

  .password-recovery-step-chip {
    flex: 0 0 auto;
    padding: 6px 10px;
    border-radius: 999px;
    text-align: center;
    font-size: .72rem;
    font-weight: 700;
    color: #f3f4f6;
    background: rgba(55, 55, 55, 0.98);
    border: 1px solid var(--pr-outline);
    text-transform: uppercase;
    letter-spacing: .06em;
  }

  .password-recovery-step-chip.is-active {
    color: #ffffff;
    background: rgba(0, 120, 212, 0.3);
    border-color: rgba(0, 120, 212, 0.76);
    box-shadow: 0 0 0 1px rgba(0, 120, 212, 0.24) inset;
  }

  .password-recovery-step-chip.is-done {
    color: #dcfce7;
    background: rgba(34, 197, 94, 0.14);
    border-color: rgba(34, 197, 94, 0.28);
  }

  .password-recovery-support-card,
  .password-recovery-feedback,
  .password-recovery-camera {
    padding: 12px;
    border-radius: 14px;
    background: linear-gradient(180deg, #3b3b3b, #343434);
    border: 1px solid var(--pr-outline-strong);
  }

  .password-recovery-support-card a {
    display: inline-block;
    margin-top: 8px;
    color: #93c5fd;
    font-weight: 700;
  }

  .password-recovery-form label {
    display: block;
    margin-bottom: 8px;
    font-weight: 700;
    color: var(--pr-text-2) !important;
  }

  .password-recovery-form__row {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    margin-bottom: 14px;
  }

  .password-recovery-feedback {
    margin-bottom: 8px;
    color: var(--pr-text-1) !important;
    display: flex;
    align-items: center;
    gap: 10px;
    min-height: 48px;
    font-size: .88rem;
  }

  .password-recovery-feedback.is-loading {
    min-height: 54px;
  }

  .password-recovery-spinner {
    width: 20px;
    height: 20px;
    border-radius: 50%;
    border: 2px solid rgba(255,255,255,.16);
    border-top-color: var(--pr-primary);
    flex: 0 0 auto;
    animation: recoverySpin .8s linear infinite;
  }

  .password-recovery-feedback.is-success {
    color: #dcfce7;
    border-color: rgba(34,197,94,.30);
    background: rgba(20, 83, 45, 0.28);
  }

  .password-recovery-feedback.is-error {
    color: #fee2e2;
    border-color: rgba(239,68,68,.30);
    background: rgba(127, 29, 29, 0.24);
  }

  .password-recovery-camera {
    margin-bottom: 8px;
    overflow: hidden;
  }

  #recovery-facial-stage-camera.is-active {
    display: grid;
    gap: 8px;
  }

  .password-recovery-security {
    margin-bottom: 14px;
    padding: 18px;
    border-radius: 16px;
    background: linear-gradient(180deg, #3b3b3b, #343434);
    border: 1px solid var(--pr-outline);
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .password-recovery-security__icon {
    width: 58px;
    height: 58px;
    border-radius: 999px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: rgba(34, 197, 94, 0.18);
    border: 1px solid rgba(34, 197, 94, 0.32);
    color: #86efac;
    font-size: 2rem;
    font-weight: 900;
    box-shadow: inset 0 1px 0 rgba(255,255,255,.08);
  }

  .password-recovery-security__content {
    display: grid;
    gap: 4px;
  }

  .password-recovery-security__title {
    display: block;
    color: #dcfce7;
    font-size: 1.02rem;
    font-weight: 700;
  }

  .password-recovery-security__summary {
    margin: 0;
    color: var(--pr-text-3) !important;
    line-height: 1.5;
  }

  .password-recovery-camera iframe {
    display: block;
    width: 100%;
    height: min(66vh, 700px);
    border: 0;
    border-radius: 12px;
    background: #05070b;
  }

  .password-recovery-modal.is-camera-stage .password-recovery-note {
    display: none;
  }

  .password-recovery-modal.is-camera-stage #recovery-step-facial > h3,
  .password-recovery-modal.is-camera-stage #recovery-step-facial > p,
  .password-recovery-modal.is-camera-stage .password-recovery-steps-bar,
  .password-recovery-modal.is-camera-stage .password-recovery-actions,
  .password-recovery-modal.is-camera-stage #recovery-facial-stage-cpf,
  .password-recovery-modal.is-camera-stage #recovery-facial-stage-loading,
  .password-recovery-modal.is-camera-stage #recovery-facial-stage-send,
  .password-recovery-modal.is-camera-stage #recovery-camera-feedback {
    display: none !important;
  }

  .password-recovery-modal.is-camera-stage #recovery-facial-stage-camera {
    display: block !important;
    margin: 0;
    padding: 0;
  }

  .password-recovery-modal.is-camera-stage #recovery-camera-wrapper {
    margin: 0;
    padding: 0;
    border: 0;
    border-radius: 12px;
    background: #020617;
  }

  .password-recovery-modal.is-camera-stage .password-recovery-steps-bar {
    justify-content: center;
    gap: 6px;
    margin-bottom: 6px;
  }

  .password-recovery-modal.is-camera-stage .password-recovery-step-chip {
    padding: 5px 9px;
    font-size: .68rem;
  }

  .password-recovery-modal.is-camera-stage #recovery-camera-feedback {
    min-height: 40px;
    margin-bottom: 6px;
  }

  .password-recovery-modal.is-camera-stage .password-recovery-camera iframe {
    height: min(78vh, 820px);
  }

  .password-recovery-actions {
    display: flex;
    justify-content: space-between;
    gap: 10px;
    margin-top: 10px;
    padding-top: 10px;
    border-top: 1px solid var(--pr-outline);
    position: sticky;
    bottom: -16px;
    background: linear-gradient(to top, rgba(47, 47, 47, 0.96), rgba(47, 47, 47, 0.76) 70%, transparent);
    backdrop-filter: blur(4px);
    z-index: 2;
  }

  .password-recovery-primary,
  .password-recovery-secondary {
    min-height: 42px;
    padding: 8px 18px;
    border-radius: 999px;
    font-weight: 700;
    letter-spacing: .01em;
    border: 1px solid transparent;
    cursor: pointer;
    box-shadow: 0 1px 2px rgba(2, 6, 23, 0.26), 0 1px 3px rgba(2, 6, 23, 0.18);
    transition: transform .18s ease, filter .18s ease, border-color .18s ease, box-shadow .18s ease, background .18s ease;
  }

  .password-recovery-primary {
    background: var(--pr-primary);
    color: #eff6ff;
  }

  .password-recovery-primary:hover:not(:disabled) {
    background: var(--pr-primary-hover);
    transform: translateY(-1px);
    box-shadow: 0 8px 18px rgba(37, 99, 235, 0.32);
  }

  .password-recovery-primary:active:not(:disabled) {
    transform: translateY(0);
    box-shadow: 0 2px 6px rgba(37, 99, 235, 0.28);
  }

  .password-recovery-primary:disabled {
    opacity: .55;
    cursor: not-allowed;
    box-shadow: none;
  }

  .password-recovery-secondary {
    background: transparent;
    color: #bae6fd;
    border-color: rgba(56, 189, 248, 0.5);
  }

  .password-recovery-secondary:hover {
    background: rgba(59, 130, 246, 0.16);
    border-color: rgba(96, 165, 250, 0.66);
    transform: translateY(-1px);
  }

  .password-recovery-primary:focus-visible,
  .password-recovery-secondary:focus-visible {
    outline: 3px solid var(--pr-focus);
    outline-offset: 2px;
  }

  .password-recovery-note {
    color: var(--pr-text-3) !important;
  }

  @keyframes recoverySpin {
    to { transform: rotate(360deg); }
  }

  @media (max-width: 720px) {
    .password-recovery-modal {
      padding: 10px;
    }

    .password-recovery-modal__dialog {
      width: min(96vw, 620px);
      max-height: 90vh;
      padding: 16px;
      border-radius: 18px;
    }

    .password-recovery-modal.is-camera-stage .password-recovery-modal__dialog {
      width: min(98vw, 720px);
      max-height: 94vh;
      padding: 10px;
    }

    .password-recovery-choice-grid,
    .password-recovery-steps-bar {
      grid-template-columns: minmax(0, 1fr);
    }

    .password-recovery-actions {
      flex-direction: column;
    }

    .password-recovery-primary,
    .password-recovery-secondary {
      width: 100%;
    }

    .password-recovery-security__grid {
      grid-template-columns: minmax(0, 1fr);
    }

    .password-recovery-camera iframe {
      height: min(58vh, 460px);
    }

    .password-recovery-modal.is-camera-stage .password-recovery-camera iframe {
      height: min(70vh, 620px);
    }
  }

  .feedback-fab {
    width: 55px !important;
    height: 55px !important;
  }
</style>



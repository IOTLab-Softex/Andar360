
<div class="rooms expanded">
  <table class="table table-salas">
    <thead class="table-secondary text-dark">
      <tr>
        <th>Foto</th>
        <th>Nome</th>
        <th>CPF</th>
        <th class="hide-sm">RG</th>
        <th class="hide-sm">Telefone</th>
        <th class="hide-sm">WhatsApp</th>
        <th class="hide-sm">Email</th>
        <th class="hide-sm">Placa</th>
        <th class="hide-sm">Publicado no App</th>
        <th class="hide-sm">Bloqueado</th>
        <th>Ações</th>
      </tr>
    </thead>
    <tbody>
      <% if @prestador_servicos.present? %>
        <% @prestador_servicos.each do |p| %>
          <tr>
            <td style="width: 80px;">
              <% if p.foto.attached? %>
                <%= image_tag url_for(p.foto), style: "width: 65px; height: 65px; border-radius: 8%; object-fit: cover; border: 1px solid #ccc;" %>
              <% else %>
                <div style="width: 65px; height: 45px; background-color: #eee; border-radius: 8%; border: 1px solid #ccc;"></div>
              <% end %>
            </td>
            <td><%= p.nome %></td>
            <td><%= p.cpf.presence || "-" %></td>
            <td class="hide-sm"><%= p.rg.presence || "-" %></td>
            <td class="hide-sm"><%= p.fone1.presence || "-" %></td>
            <td class="hide-sm"><%= p.whatsapp.presence || "-" %></td>
            <td class="hide-sm"><%= p.email.presence || "-" %></td>
            <td class="hide-sm"><%= p.placa.presence || "-" %></td>
            <td class="hide-sm"><%= p.publicado_no_app? ? "Sim" : "Não" %></td>
            <td class="hide-sm"><%= p.bloqueado? ? "Sim" : "Não" %></td>
            <td>
              <%= link_to prestador_servico_path(p), class: "btn btn-success" do %>
                <i class="fa-solid fa-eye"></i> <div class="hide-sm"> Ver </div>
              <% end %>
              <%= link_to edit_prestador_servico_path(p), class: 'btn edit' do %>
                <i class="fa-solid fa-pen-to-square"></i> <div class="hide-sm"> Edite </div>
              <% end %>
              <%= button_to prestador_servico_path(p),
                  method: :delete,
                  data: { turbo_confirm: 'Tem certeza que deseja excluir?' },
                  class: 'btn btn-warning' do %>
                <i class="fa-solid fa-trash"></i> <div class="hide-sm"> Excluir </div>
              <% end %>
            </td>
          </tr>
        <% end %>
      <% else %>
        <tr>
          <td colspan="10">Nenhum prestador de serviço encontrado.</td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>


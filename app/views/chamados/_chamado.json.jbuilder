json.extract! chamado, :id, :os, :unidade, :titulo, :prioridade, :status, :data_resolucao, :exibir_no_app, :local, :responsavel, :observacao, :created_at, :updated_at
json.url chamado_url(chamado, format: :json)
